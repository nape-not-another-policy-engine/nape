name: NAPE - Continuous Integration (CI) Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  source-code-integration:
    uses: nape-not-another-policy-engine/nape-build-deploy-release/.github/workflows/rust-standards-ci-workflow.yml@main
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}